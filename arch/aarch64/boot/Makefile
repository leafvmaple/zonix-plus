# ==========================================================================
# arch/aarch64/boot/Makefile — AArch64 boot rules
#
# AArch64 does not use a separate BIOS/UEFI bootloader chain.
# The kernel ELF is loaded directly by QEMU -kernel or by U-Boot.
# This file is intentionally minimal.
# ==========================================================================
