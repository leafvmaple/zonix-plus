# ==========================================================================
# arch/aarch64/boot/Makefile — AArch64 boot rules
#
# Dispatches to the UEFI bootloader build.
# ==========================================================================

BOOT_INC := -I include -I arch/$(ARCH)/include

include arch/$(ARCH)/boot/uefi/Makefile
