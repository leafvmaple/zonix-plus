#pragma once

/**
 * @file segments.h
 * @brief AArch64 stub for x86 segment descriptor definitions.
 *
 * AArch64 does not use segmentation. This header is provided
 * so shared code that conditionally references it can compile.
 */
