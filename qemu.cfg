# QEMU Configuration for Zonix OS - IDE Mode
# Usage: qemu-system-i386 -readconfig qemu.cfg

[machine]
type = "pc"

[memory]
size = "32M"

[boot-opts]
order = "c"

# Primary IDE disk (hda) - Main system disk
[drive]
file = "bin/zonix.img"
format = "raw"
if = "ide"
index = "0"
media = "disk"

# Secondary IDE disk (hdb) - Test disk
# [drive]
# file = "bin/fat32_test.img"
# format = "raw"
# if = "ide"
# index = "1"
# media = "disk"
